// import React, { useContext } from "react";
// import { TimeContext } from "../context/TimeContext";

// const DailyRemainer = () => {
//   const timeLeft = useContext(TimeContext);

//   // Assuming you want to display the remaining time in a specific way for DailyRemainer
//   return (
//     <div>
//       {timeLeft.map((quest) => (
//         <p key={quest.id}>
//           Daily Info: {quest.info} -{" "}
//           {`${quest.timeLeft.days}d ${quest.timeLeft.hours}h ${quest.timeLeft.minutes}m ${quest.timeLeft.seconds}s`}
//         </p>
//       ))}
//     </div>
//   );
// };

// export default DailyRemainer;
